// Function to handle profile view
function viewProfile(name) {
    alert("Viewing profile of: " + name);
    // Yahan aap kisi dusre page ka link bhi daal sakte hain
    // window.location.href = `/profile/${name.toLowerCase()}`;
}

console.log("Team profiles loaded successfully!");